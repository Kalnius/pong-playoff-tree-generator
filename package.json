{
  "name": "table-tennis-playoff-form",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix static/playoff-ui run dev",
    "build": "npm --prefix static/playoff-ui run build",
    "preview": "npm --prefix static/playoff-ui run preview"
  }
}